'use strict';

/**
 * codecomposerstudio router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::codecomposerstudio.codecomposerstudio');
