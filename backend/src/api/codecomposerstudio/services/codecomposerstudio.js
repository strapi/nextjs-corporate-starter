'use strict';

/**
 * codecomposerstudio service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::codecomposerstudio.codecomposerstudio');
